{
  
  "dependencies": {
    "arabic-reshaper": "^1.1.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "scripts": {
    "build": "parcel build index.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}
